# Copy to frontend/.env (gitignored). Never commit secrets or production-only URLs with tokens.
#
# Local API base (no trailing slash on the host path segment — keep /api as shown)
VITE_API_BASE_URL=http://localhost:8000/api
